/**
 * Console app entry point.
 *
 * Team TODO:
 * - Compose dependencies (repositories + services + controllers)
 * - Start the main CLI flow
 *
 * Note: CLI output/messages should be in Spanish.
 */

export {};
