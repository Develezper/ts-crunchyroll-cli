# Guía Rápida

Este archivo es un acceso rápido para el equipo.
La guía principal y completa del proyecto está en:

- `TEAM_GUIDE_CRUD_CRUNCHYROLL.md`

## Qué revisar primero

1. Roles y ownership por archivo.
2. Estado actual (qué está implementado y qué falta).
3. Flujo Git y protocolo de conflictos.
4. Checklist final de entrega.

## Comandos básicos

```bash
git fetch origin
git switch main
git pull --ff-only origin main
git switch <tu-rama>
git pull --ff-only origin <tu-rama>
```

```bash
npx tsc --noEmit
bun run src/index.ts
```
